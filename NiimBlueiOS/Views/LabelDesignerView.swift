import SwiftUI

struct LabelDesignerView: View {
    var body: some View {
        Text("Дизайнер меток")
            .font(.largeTitle)
            .padding()
    }
}

#Preview {
    LabelDesignerView()
}
